# My kitty customizations — loaded AFTER the dots-hyprland base config.
# Later settings override earlier ones, so anything here takes priority.

shell zsh

# Add any other customizations below:
# font_size 12
# cursor_shape block
# background_opacity 0.95
